#include<stdio.h>
int main(){
    int driver ;
    scanf("%d",age)
    if (driver>18){
        if (driver == licence){
            if (driver != drunk){
                printf("can drive");
            }
        }
    }
    else {
        printf("driver is not eligable for driving");
    }
return 0;}

